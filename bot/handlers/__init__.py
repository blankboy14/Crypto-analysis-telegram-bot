"""
bot/handlers/

One module per Phase-1.2 menu button, plus the shared Spot/Future/Both
prompt (market_select.py) that market_analyse.py, strong_signal.py,
and search_signal.py all reuse instead of building their own keyboard.
"""